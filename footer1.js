var con = getElementsByClass('pagecontainer');
var mvobj = document.getElementById('footer'); //footer can be defined as promotion box
var obj = getElementsByClass('stBadge');
con[0].insertBefore(mvobj,obj[0]);
